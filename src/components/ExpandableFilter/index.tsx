import React from "react";
// import styled from 'styled-components';
// import Checkbox from "../checkbox";

const ExpandableFilter: React.FC = () => {
  const [filtersShown, setFiltersShown] = React.useState(false);

  // TODO You need to create your own checkbox component with a custom checkmark
  return <div>ExpandableFilter</div>;
};

export default ExpandableFilter;
